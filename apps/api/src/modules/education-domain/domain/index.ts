/**
 * Gate 1A intentionally exposes only the module boundary.
 * CourseRun, EvidenceObservation, EvidenceClaim and interaction contracts
 * start in Gate 1B.
 */
export const educationDomainGate = "gate-1b" as const;
