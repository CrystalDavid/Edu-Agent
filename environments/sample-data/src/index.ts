/**
 * Stable synthetic data used by the local product demo and by tests that
 * exercise the same demo slice. This package must not contain assertions,
 * test runners, fake-provider behavior, or real school/student data.
 */
export * from "./gate2.js";
